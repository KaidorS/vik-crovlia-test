<header class="bg-white shadow-sm border-b border-gray-200">
    <div class="container mx-auto px-4 py-4 max-w-5xl flex justify-between items-center">
        <a href="{{ route('news.index') }}" class="text-2xl font-bold text-gray-800 hover:text-blue-600 transition">
            VIK Crovlia Test
        </a>
        <nav class="space-x-4">
            <a href="{{ route('news.index') }}" class="text-gray-600 hover:text-blue-600">Все новости</a>
        </nav>
    </div>
</header>
